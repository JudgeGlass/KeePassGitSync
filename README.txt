Need to install Python 3.10+

For windows:
- Install python3.10+
- Run `pip install --user tk git-python`

For linux: 
- Install python3.10+
- Run `pip install --user tk git-python`
- `sudo apt-get install python3-tk`